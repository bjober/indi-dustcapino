NDI DustCapIno – Restore State

Datum: 2026-02-27
Status: Dustcap OK, Lightbox delvis implementerad

SYSTEM

OS: Ubuntu 24.04
INDI: Byggd från source
Build prefix: /usr/local

Driver startas med:

indiserver -vv ./indi_dustcapino

Build-kommando:

cd ~/indi-dustcapino/build
rm -rf *
cmake .. -DCMAKE_INSTALL_PREFIX=/usr/local
make -j4
SERIAL (Fungerar)

Standard:

Port: /dev/ttyUSB2
Baud: 115200

Connection fungerar och loggar:

[INFO] Connected to /dev/ttyUSB2 @ 115200
DUSTCAP (Fungerar 100%)

Property: CAP_PARK
Kommandon till Arduino:

CMD:OPEN
CMD:CLOSE

Arduino svarar:

MOVING
IDLE_OPEN
IDLE_CLOSED

TimerHit hanterar:

IDLE_OPEN → IPS_OK + UNPARK

IDLE_CLOSED → IPS_OK + PARK

State uppdateras korrekt.

LIGHTBOX (Delvis)

Property: FLAT_LIGHT_CONTROL

Den:

Definieras efter connect

Tas bort vid disconnect

Ligger just nu i egen flik

Saknar full ISNewSwitch-logik

Saknar korrekt state-återkoppling

Måste:

Kopplas i ISNewSwitch

Skicka:

CMD:LIGHT_ON
CMD:LIGHT_OFF

Ta emot:

LIGHT_ON
LIGHT_OFF

Uppdatera IPS_BUSY → IPS_OK i TimerHit

Viktiga funktioner som fungerar

Serial med DEVICE_PORT + BAUD_RATE

CAP_PARK via ISNewSwitch

TimerHit med tty_read_section

State machine korrekt

Connection stabil

Nästa steg i ny chat

Mål:

✔ Lightbox ska fungera exakt som CAP_PARK
✔ Rätt tab (MAIN_CONTROL_TAB eller egen)
✔ Busy → Ok state korrekt
✔ Inte bryta serial
✔ Inte bryta dustcap

Om något går sönder

Verifiera alltid:

indiserver -vv ./indi_dustcapino

och kontrollera:

defSwitchVector FLAT_LIGHT_CONTROL

TX loggar

RX loggar

KLAR STATUS:
Projektet är stabilt.
Endast Lightbox behöver färdigställas.